# Tajný kľúč – frontend ho posiela v hlavičke X-StockPilot-Key (VITE_BACKEND_SECRET_KEY).
# Musia byť zhodné. Bez tejto premennej je kontrola v dev vypnutá.
BACKEND_SECRET_KEY=tvoje-super-tajne-heslo-123

# Databáza (Coolify / Docker)
# DATABASE_URL=postgresql://user:password@host:5432/dbname

# Port (voliteľné)
# PORT=3000

# Produkcia
# NODE_ENV=production
